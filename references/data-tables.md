# 数据表格查询、展示与导航交互规范

适用于只读报表、带单行操作的管理列表和显式启用批量能力的数据表格。本文件是数据表格查询、展示、导航、列、语义与焦点规则的 owner；筛选字段自身的值、校验与错误继续由 [表单规范](forms.md) 负责，自绘 Select / Combobox 的草稿、提交值和弹层由 [Select / Combobox 规范](selects-comboboxes.md) 负责，跨端布局由 [响应式与自适应规范](responsive-adaptive.md) 负责。

本阶段只建立选择和操作能力所依赖的查询与展示基础。选择范围、批量执行终态、响应式等价转换及 route/unmount 生命周期在后续规则中扩展，不能从本文件未定义的细节自行推断。

## 范围、排除项与规则记法

第一版覆盖数据表格结构、能力档位、状态分组、筛选、排序、页码分页、上一页/下一页游标分页、加载与错误状态、列显示/固定/受限宽度、原生 Table / ARIA Grid 选择条件、键盘模型和焦点恢复。

第一版排除行内编辑、树形表格、拖拽行排序、透视表、加载更多、无限滚动、用户拖拽改变列顺序和个人布局持久化。

下表和后续各表中的“规则”列是本阶段硬规则全集。每条规则都具有稳定 ID，并在“验收”列映射到本文件末尾的可执行检查；新增硬规则时要同时新增或扩展对应验收。

| ID | 规则 | 验收 |
| --- | --- | --- |
| `DT-SCOPE-01` | 第一版只提供上列能力；排除项不得以隐藏配置、实验入口或未声明键盘路径混入。 | `A01` |
| `DT-SCOPE-02` | 表格 owner 只接收筛选控件已经提交的业务值；字段内部草稿、校验、Select 查询和 popup 状态仍归对应专项 owner。 | `A05`、`A06` |

## 能力档位

每个表格实例以 `capabilityTier` 显式声明一个档位；权限解析后的 `resolvedTier` 才决定可渲染能力。

| ID | 规则 | 验收 |
| --- | --- | --- |
| `DT-CAP-01` | `capabilityTier` 只能是 `display`、`row-action` 或 `bulk-action`，不得按页面名称、是否出现复选框或后端返回内容临时猜测。 | `A01` |
| `DT-CAP-02` | `display` 只展示、查询与导航；不得渲染选择列、行选择控件、批量入口或空批量工具栏。 | `A01` |
| `DT-CAP-03` | `row-action` 可以提供行跳转、详情或单行操作，但不得因此获得多选、表头全选或批量工具栏。行跳转与行内控件保持独立，不能把含多个控件的整行包装成一个大链接。 | `A01`、`A18` |
| `DT-CAP-04` | 只有 `bulk-action` 可以渲染选择与批量操作的结构；本阶段仅保留对应状态槽，不定义选择范围或执行终态。 | `A01`、`A03` |
| `DT-CAP-05` | 权限变化可以把实例降到更低档位；提交降级时一次性移除所有越权入口、选择列和空工具栏，清理它们持有的交互状态，并把失去目标的焦点按 `DT-FOC-02` 迁移。不得只禁用控件而留下无恢复路径的界面。 | `A02`、`A21` |

## 状态模型与查询提交门禁

表格实例使用稳定 `ownerId`，并维护以下四组正交状态。字段名可以按框架适配，但语义和转换不能合并。

| 状态组 | 最少内容 |
| --- | --- |
| 查询状态 `queryState` | `appliedFilters`、`sortRules`、`pagination`、`pageSize`、查询快照 `querySnapshot`、`snapshotId`、可选 `datasetVersion`、请求代次 `requestGeneration`、`requestPhase`、`queryError`、`stale` |
| 展示状态 `viewState` | `visibleColumnIds`、`pinnedColumnIds`、`columnWidths`、`density`、当前结果行与结果摘要 |
| 交互状态 `interactionState` | `focusIntent`、当前焦点的 `recordId` / `columnId`、展开行、打开的单行操作、已声明但尚未扩展的选择槽 |
| 操作状态 `operationState` | 当前单行或批量意图的阶段、受影响记录标识、错误 owner 与恢复入口；详细执行契约留给后续规则 |

| ID | 规则 | 验收 |
| --- | --- | --- |
| `DT-STATE-01` | 四组状态分别拥有写入者；查询响应不得直接改写草稿筛选，列变化不得隐式改查询，焦点变化不得改业务结果，操作反馈不得伪装成查询错误。 | `A03` |
| `DT-STATE-02` | `requestPhase` 至少区分 `idle`、`initial-loading`、`ready`、`refreshing`、`initial-error` 和 `refresh-error`；不得用单一 `loading` / `error` 同时代表首次加载、刷新和操作状态。 | `A03`、`A13`、`A14` |
| `DT-REQ-01` | 每次准备发出查询时，先把已应用筛选、完整排序（含稳定次序键及空值/区域规则）、分页模式与位置、页大小、权限范围和适用数据集版本冻结为不可变 `querySnapshot`，生成 `snapshotId`，再把 `requestGeneration` 严格加一。发出后任何状态变化不得修改该快照。 | `A04`、`A05` |
| `DT-REQ-02` | 查询响应只有同时满足 `(a)` 当前 owner 仍 live 且 `ownerId` 相同、`(b)` 响应 generation 等于当前 `requestGeneration`、`(c)` 响应 `snapshotId` 等于当前 `snapshotId` 时，才可提交行、总数/游标、错误、`requestPhase` 或 `stale`。任一不匹配都只记录 `response-discarded`。 | `A04` |
| `DT-REQ-03` | 取消旧请求只用于节省资源，不能替代 `DT-REQ-02` 的三重提交门禁；已取消但迟到的响应仍需经过同一判断。 | `A04` |
| `DT-REQ-04` | 应用/移除筛选、提交排序、翻页、改变页大小、被接受的显式刷新或失效页恢复都建立新快照并递增代次；单纯编辑筛选草稿、改变焦点、开关行详情或调整列展示不发查询。 | `A05`、`A06`、`A16` |
| `DT-REQ-05` | 显式刷新先根据已应用筛选、排序、分页、页大小、权限范围、适用数据版本和刷新原因形成查询意图键；同一 in-flight 查询意图在创建快照前合并到当前工作，不新增 `snapshotId`、`requestGeneration`、请求或开始公告。当前意图终结或意图键变化后，下一次刷新才可被接受。 | `A05`、`A13` |

## 筛选

| ID | 规则 | 验收 |
| --- | --- | --- |
| `DT-FIL-01` | `filterDraft` 与 `appliedFilters` 分离；草稿只接收字段 owner 已提交、但尚未应用到查询的业务值，不接收 Select 查询、active option 等控件内部状态。结果标题、数量、请求参数和 URL 只反映 `appliedFilters`。 | `A06` |
| `DT-FIL-02` | 每个筛选器在产品配置中声明 `applyMode: immediate | explicit`。低成本简单筛选可在已提交值变化后即时应用；复杂或多字段筛选使用明确“应用”操作。两种模式都只能在字段 owner 提交合法业务值后创建查询。 | `A06` |
| `DT-FIL-03` | “重置”恢复产品声明的 `defaultFilters`，而不是含糊地清空所有值；只有重置后的条件与当前 `appliedFilters` 语义不同时才发新查询。 | `A06` |
| `DT-FIL-04` | 所有已应用条件持续可见、具可访问名称并可单独移除；移除也是一次应用事件。隐藏在折叠区域的条件仍要在摘要中可发现。 | `A07` |
| `DT-FIL-05` | 只有明确标记 `urlSafe` 的已应用条件可序列化到 URL；账号、令牌、个人识别、自由文本中的敏感值和产品标记 `sensitive` 的条件不得进入路径、查询串、片段、标题或分析日志。 | `A06` |
| `DT-FIL-06` | 应用、移除或有效重置筛选时，页码分页回到第 1 页，游标分页回到初始游标，然后创建新查询快照；界面同时更新可见条件摘要，不能在旧结果上静默显示新条件已生效。 | `A05`、`A07` |

## 排序

| ID | 规则 | 验收 |
| --- | --- | --- |
| `DT-SORT-01` | 表格配置声明可排序列、默认排序、当前方向以及 `single` 或 `multiple` 模式；多列模式同时显示并播报优先级，单列模式提交新列时替换旧业务排序。 | `A08`、`A09` |
| `DT-SORT-02` | 不可排序列不渲染排序按钮，也不设置 `aria-sort`。可排序表头使用有可访问名称的真实按钮；原生表格只在当前主排序表头设置正确 `aria-sort`，其他表头不伪造状态。 | `A09` |
| `DT-SORT-03` | 所有分页查询使用稳定排序：业务排序键之后追加不重复且不可变的稳定次序键。产品/后端同时声明空值位置、大小写及区域/自然排序规则，并把它们冻结进快照；不得依赖当前数据库偶然顺序。 | `A08` |
| `DT-SORT-04` | 提交不同排序时，页码分页回到第 1 页、游标分页回到初始游标，再创建新快照；只有全部页面绑定同一不可变 `datasetVersion` 或等价服务端快照时，才可断言稳定排序后的跨页记录无重复或遗漏。 | `A05`、`A08` |
| `DT-SORT-05` | 页间响应的数据版本与首个已接受页面不一致时，当前分页链立即失效并标为 `stale`，停止拼接或继续导航，回到第一个有效位置重新开始。服务端不提供一致性版本/快照能力时，界面不得承诺跨页零重复遗漏；检测到页间数据变化后采用同一失效与重启路径。 | `A08` |

## 分页

| ID | 规则 | 验收 |
| --- | --- | --- |
| `DT-PAG-01` | 每个实例显式选择 `numbered` 页码分页或 `cursor` 游标分页；同一查询快照内不得混用两种位置语义。 | `A10`、`A11` |
| `DT-PAG-02` | 页码分页只在总数可靠时提供直接页码和跳转；可见当前页、总页数与结果范围，首页/末页边界控件使用原生 `disabled`。请求页超过服务端最新末页时执行 `DT-PAG-05`。 | `A10` |
| `DT-PAG-03` | 游标分页只提供接口支持的“上一页”和“下一页”，保存每个方向所需游标；没有对应游标时原生禁用该方向。不得虚构总页数、随机跳页、加载更多或无限滚动。 | `A11` |
| `DT-PAG-04` | 改变 `pageSize` 后回到第一个有效位置并建立新快照；控件显示当前值和可访问名称，服务端拒绝的值不能成为当前页大小。 | `A12` |
| `DT-PAG-05` | 删除或数据变化使当前页失效时，页码模式请求最近有效页（不小于 1），游标模式回退到服务端返回的最近有效前向位置或初始游标；恢复只执行一次，成功后简短播报新位置，不能在空的无效页循环重试。 | `A12` |

## 首次加载、刷新、过期、错误与空状态

结果容器是查询状态和查询错误的 primary owner。筛选字段错误仍归字段；单行或批量操作错误不写入 `queryError`。

| ID | 规则 | 验收 |
| --- | --- | --- |
| `DT-DATA-01` | 仅在首次加载且没有可用结果时显示与最终表格结构相符的骨架或占位；结果容器设置 `aria-busy="true"`，不得渲染可操作的假数据。 | `A13` |
| `DT-DATA-02` | 刷新时保留上次成功的行、列、分页上下文和焦点意图，并设置 `requestPhase: refreshing`、`aria-busy="true"`；同一 in-flight 查询意图按 `DT-REQ-05` 在创建快照前合并，旧行不能伪装成新响应。 | `A13` |
| `DT-DATA-03` | 首次加载失败且没有可用结果时进入 `initial-error`，以结果区域内的文本错误和可聚焦重试入口替代不可用表格；重试创建新代次。 | `A14` |
| `DT-DATA-04` | 刷新失败时保留旧行并进入 `refresh-error`，设置 `stale: true`，显示“数据可能已过期”的结果区域消息和重试入口；不得清空表格、分页或焦点上下文。 | `A14` |
| `DT-DATA-05` | 已应用筛选产生零结果时显示“当前条件无匹配”并提供调整或清除筛选入口；数据源本身为空时显示与能力档位相符的独立空数据集状态。两者不得共用含糊的“暂无数据”。 | `A15` |
| `DT-DATA-06` | `[a]` 每个被接受的查询开始由结果 owner 公告一次；`[b]` 每个已接受且改变结果数量或位置的响应由结果 owner 公告一次；`[c]` 每次成功的失效页恢复由结果 owner 公告一次；`[d]` 每个已接受的查询失败由结果 owner 公告一次；`[e]` 同一完整错误消息不得同时由筛选字段、结果摘要和全局 live region 重复播报。 | `A12`、`A13`、`A14`、`A15` |

## 列显示、固定与宽度

| ID | 规则 | 验收 |
| --- | --- | --- |
| `DT-COL-01` | `visibleColumnIds`、`pinnedColumnIds` 和 `columnWidths` 只引用稳定 `columnId`；隐藏列从视觉、表头关联和键盘模型中一致移除，重新显示时恢复正确语义而不是复用陈旧 DOM 引用。 | `A16` |
| `DT-COL-02` | 承担记录身份、主要任务或唯一恢复入口的关键列标记 `requiredVisible`，不得隐藏；列控制器以禁用状态和可感知原因表达这一限制。 | `A16` |
| `DT-COL-03` | 隐藏承载当前业务排序、焦点或已打开单行操作的列前，先显式取消或迁移相应依赖；无法得到安全目标时拒绝隐藏并说明原因。 | `A16`、`A21` |
| `DT-COL-04` | 固定列在容器横向滚动、缩放和长内容下不得遮挡当前焦点、菜单、错误、相邻数据或滚动边界；检测到遮挡时减少固定列或解除最低优先级固定状态，并保留列可见。 | `A17` |
| `DT-COL-05` | 可调宽列声明 `minWidth`、`maxWidth` 和离散键盘步长。指针拖动与可聚焦调整柄的方向键操作都限制在边界内；`Home` / `End` 分别设到最小/最大值，并以可访问值暴露结果。 | `A17` |
| `DT-COL-06` | 第一版不提供拖拽改变列顺序或个人布局持久化；密度、显示、固定和宽度变化不得创建查询快照或重新请求数据。 | `A01`、`A16`、`A17` |

## 原生 Table、ARIA Grid 与键盘

| ID | 规则 | 验收 |
| --- | --- | --- |
| `DT-SEM-01` | 普通只读表格和含按钮、链接、菜单触发器等常规控件的数据表格使用原生 `<table>`；提供可访问名称/说明，使用 `<th scope="col|row">` 或等价 header 关联，不能仅为样式改成 Grid。 | `A18` |
| `DT-SEM-02` | 原生 Table 不接管方向键、Home、End、Page Up 或 Page Down；Tab 顺序只进入真实交互控件并按 DOM / 视觉任务顺序前进，静态单元格不额外设 `tabindex="0"`。 | `A18` |
| `DT-SEM-03` | 只有产品确实需要单元格级二维键盘导航或单元格选择时才使用 ARIA Grid；采用 Grid 就要实现完整键盘、焦点与行列语义，不得只贴 `role="grid"`。 | `A19` |
| `DT-SEM-04` | ARIA Grid 使用单一 Tab 入口和一个活动单元格：左右键移同一行相邻单元格，上下键移同列相邻行，Home / End 移到本行首/末单元格，Ctrl/Command + Home / End 移到全表首/末单元格，Page Up / Page Down 按一个可视页移动同列并在边界截停；每次只产生一个活动目标。 | `A19` |
| `DT-SEM-05` | Grid 单元格含按钮或菜单触发器时，Enter 或 F2 把焦点移入该控件，Escape 返回该单元格导航模式；控件获得焦点后保持按钮或菜单的原生键盘语义，Grid 不得吞掉其激活键、菜单导航键或 Escape。Tab / Shift+Tab 从 Grid 的单一入口离开，不逐个遍历静态单元格。 | `A19` |

## 焦点意图与恢复

`focusIntent` 至少记录 `{sourceEvent, recordId?, columnId?, controlId?, fallbackId}`。记录和列都使用数据层稳定标识，不能用当前数组索引或第几个 DOM 子节点恢复焦点。

| ID | 规则 | 验收 |
| --- | --- | --- |
| `DT-FOC-01` | 后台刷新或同页结果重取后，原 `recordId + columnId + controlId` 仍存在且可聚焦时保持或仅一次恢复该目标；不得因为行排序位置变化而聚焦相同索引的另一条记录。 | `A20` |
| `DT-FOC-02` | 精确目标消失时，按已记录策略依次选择同记录等价控件、同列最近记录、结果摘要/标题或当前分页控件；最终目标必须存活、可感知且与事件相关，不能落到 `document.body`、文档根节点或已移除元素。 | `A20`、`A21` |
| `DT-FOC-03` | 编辑筛选草稿、应用筛选和提交排序时，发请求本身不抢走仍存活的触发控件焦点；若结果提交移除了焦点目标，才按 `DT-FOC-02` 迁移一次。 | `A21` |
| `DT-FOC-04` | 用户触发翻页时记录分页来源；匹配响应提交后，焦点移动一次到结果摘要/标题，除非产品显式保留了仍存活且语义未变的分页触发器。失效页自动恢复不得产生第二次抢焦点。 | `A10`、`A11`、`A12`、`A21` |

## 验证与报告边界

| ID | 规则 | 验收 |
| --- | --- | --- |
| `DT-REPORT-01` | 未实际执行浏览器、辅助技术、键盘、真实数据竞态或真实组件运行时检查时，对应项目必须标为“未验证”并列出所需环境；静态文本检查不得写成运行时通过。 | `A22` |

## 硬规则分句覆盖清单

本清单按语义而不是标点拆分独立义务和禁止项；clause ID 只用于覆盖审计，不新增或弱化上文规则。人工审查要逐项核对“可观察分句”与对应验收中的状态、DOM/ARIA 或事件计数；结构脚本只证明规则、clause 和验收引用没有丢失，不能替代这项语义审查。

| Clause ID | 可观察分句 | 验收观察点 |
| --- | --- | --- |
| `DT-SCOPE-01.a`、`DT-SCOPE-01.b`、`DT-SCOPE-01.c`、`DT-SCOPE-01.d` | 只提供首版能力；排除能力不得经隐藏配置、实验入口或未声明键盘路径进入。 | `A01`：八项排除能力分别检查 DOM、handler、请求和状态写入。 |
| `DT-SCOPE-02.a`、`DT-SCOPE-02.b`、`DT-SCOPE-02.c`、`DT-SCOPE-02.d` | 表格只接收已提交业务值；字段内部草稿、校验、Select 查询和 popup 分别留在专项 owner。 | `A05` 查询矩阵；`A06` 内部草稿与非法值均不发请求。 |
| `DT-CAP-01.a`、`DT-CAP-01.b`、`DT-CAP-01.c`、`DT-CAP-01.d` | 档位仅三值；不得按页面名、复选框或响应内容推断。 | `A01` 三个显式实例的 `tier-resolved` 与 DOM 能力逐项核对。 |
| `DT-CAP-02.a`、`DT-CAP-02.b`、`DT-CAP-02.c`、`DT-CAP-02.d`、`DT-CAP-02.e` | `display` 仅展示/查询/导航；无选择列、行选择、批量入口或空工具栏。 | `A01` 的 `display` 状态与 DOM 五项断言。 |
| `DT-CAP-03.a`、`DT-CAP-03.b`、`DT-CAP-03.c`、`DT-CAP-03.d`、`DT-CAP-03.e`、`DT-CAP-03.f` | `row-action` 只增加行跳转/详情/单行操作；无多选、表头全选、批量工具栏；行跳转与控件独立且整行不是大链接。 | `A01` 档位 DOM；`A18` 各控件只触发自身动作。 |
| `DT-CAP-04.a`、`DT-CAP-04.b`、`DT-CAP-04.c`、`DT-CAP-04.d` | 选择/批量结构只属于 `bulk-action`；本阶段只留状态槽，不定义范围或执行终态。 | `A01` 三档结构；`A03` 操作状态只记录当前意图槽。 |
| `DT-CAP-05.a`、`DT-CAP-05.b`、`DT-CAP-05.c`、`DT-CAP-05.d`、`DT-CAP-05.e`、`DT-CAP-05.f`、`DT-CAP-05.g` | 权限可降级；越权入口、选择列、空工具栏和交互状态原子移除；焦点迁移；不留下无恢复 disabled 控件。 | `A02` 每次降级的状态、DOM、ARIA 与焦点计数；`A21` 目标消失。 |
| `DT-STATE-01.a`、`DT-STATE-01.b`、`DT-STATE-01.c`、`DT-STATE-01.d`、`DT-STATE-01.e` | 四组状态写入入口可区分；查询、列、焦点、操作不得跨 owner 非法写入。 | `A03` 五类事件的转换审计与错误 owner。 |
| `DT-STATE-02.a`、`DT-STATE-02.b` | 六个请求阶段可区分；单一 loading/error 不得代替它们。 | `A03`、`A13`、`A14` 的完整阶段序列与审计字段。 |
| `DT-REQ-01.a`、`DT-REQ-01.b`、`DT-REQ-01.c`、`DT-REQ-01.d` | 快照冻结全部查询输入；生成 snapshotId；generation 加一；发出后不可变。 | `A04` 快照竞态；`A05` 被接受事件各一次创建与递增。 |
| `DT-REQ-02.a`、`DT-REQ-02.b`、`DT-REQ-02.c`、`DT-REQ-02.d`、`DT-REQ-02.e` | live owner、generation、snapshot 三门禁同时匹配才可提交全部响应字段；不匹配只丢弃。 | `A04` 乱序、旧 owner 与取消后迟到响应的提交/丢弃日志。 |
| `DT-REQ-03.a`、`DT-REQ-03.b`、`DT-REQ-03.c` | 取消只省资源；不承担正确性；取消后迟到响应仍过三门禁。 | `A04` 已取消 `S1` 再送达仍只记录 discarded。 |
| `DT-REQ-04.a`、`DT-REQ-04.b`、`DT-REQ-04.c`、`DT-REQ-04.d`、`DT-REQ-04.e` | 六类被接受查询事件建快照/增代次；草稿、焦点、详情和列展示不查询。 | `A05` 查询/非查询事件矩阵；`A06`、`A16` 的请求计数。 |
| `DT-REQ-05.a`、`DT-REQ-05.b`、`DT-REQ-05.c`、`DT-REQ-05.d` | 意图键包含全部查询范围与刷新原因；同一 in-flight 意图在建快照前合并；四项计数不变；终结或键变化后才接受。 | `A05` 双触发与终结后刷新；`A13` 点击/Enter/重放的 snapshot、generation、请求、公告计数。 |
| `DT-FIL-01.a`、`DT-FIL-01.b`、`DT-FIL-01.c`、`DT-FIL-01.d` | draft/applied 分离；草稿仅接收已提交业务值；不接收 Select 内部状态；结果/URL 只反映 applied。 | `A06` 值、请求、摘要和 URL 对比。 |
| `DT-FIL-02.a`、`DT-FIL-02.b`、`DT-FIL-02.c`、`DT-FIL-02.d` | 每个筛选声明模式；immediate 只接已提交值；复杂筛选显式应用；非法值不查询。 | `A06` immediate、explicit 与非法值三条路径。 |
| `DT-FIL-03.a`、`DT-FIL-03.b`、`DT-FIL-03.c` | 重置回默认；不含糊清空；语义未变不查询。 | `A06` 两次重置及请求数。 |
| `DT-FIL-04.a`、`DT-FIL-04.b`、`DT-FIL-04.c`、`DT-FIL-04.d`、`DT-FIL-04.e` | 已应用条件可见、有名称、可单独移除；移除即应用；折叠条件仍可发现。 | `A07` 三条件 DOM、移除与分页/请求日志。 |
| `DT-FIL-05.a`、`DT-FIL-05.b` | 仅 urlSafe 可序列化；敏感值不得进入六类外部表面。 | `A06` URL、标题与分析事件的敏感值扫描。 |
| `DT-FIL-06.a`、`DT-FIL-06.b`、`DT-FIL-06.c`、`DT-FIL-06.d`、`DT-FIL-06.e` | 筛选变更分别重置页码/游标并建快照；摘要同步；旧行不冒充新条件结果。 | `A05` 起点和快照；`A07` 摘要与移除事件。 |
| `DT-SORT-01.a`、`DT-SORT-01.b`、`DT-SORT-01.c` | 声明列/默认/方向/模式；多列显示并公告优先级；单列替换旧排序。 | `A08` 快照；`A09` single/multiple DOM 与事件。 |
| `DT-SORT-02.a`、`DT-SORT-02.b`、`DT-SORT-02.c`、`DT-SORT-02.d`、`DT-SORT-02.e` | 不可排序列无按钮/aria-sort；可排序列有具名按钮；仅主列 aria-sort；其余不伪造。 | `A09` 每列表头 DOM/ARIA 与请求计数。 |
| `DT-SORT-03.a`、`DT-SORT-03.b`、`DT-SORT-03.c`、`DT-SORT-03.d`、`DT-SORT-03.e` | 追加稳定键；键唯一且不可变；声明空值/大小写/区域；冻结快照；不依赖偶然顺序。 | `A08` V1 重复查询的排序与快照日志。 |
| `DT-SORT-04.a`、`DT-SORT-04.b`、`DT-SORT-04.c`、`DT-SORT-04.d` | 排序分别重置页码/游标并建快照；零重复遗漏只对同一不可变版本成立。 | `A05` 排序起点；`A08` 仅 V1 计算并集/交集。 |
| `DT-SORT-05.a`、`DT-SORT-05.b`、`DT-SORT-05.c`、`DT-SORT-05.d`、`DT-SORT-05.e` | 版本失配令链失效/stale；停止拼接导航；从起点重启；无版本不承诺完整性；检测变化走同一路径。 | `A08` V2 与无版本变化两场景的失效、stale、append=0、重启计数。 |
| `DT-PAG-01.a`、`DT-PAG-01.b` | 显式选择分页模式；同一快照不混用。 | `A10` numbered 与 `A11` cursor 结构/请求。 |
| `DT-PAG-02.a`、`DT-PAG-02.b`、`DT-PAG-02.c`、`DT-PAG-02.d` | 仅可靠总数直接定位；显示页/总页/范围；边界原生 disabled；越界恢复。 | `A10` 三位置 DOM/请求；`A12` 失效页。 |
| `DT-PAG-03.a`、`DT-PAG-03.b`、`DT-PAG-03.c`、`DT-PAG-03.d` | 游标仅上/下页并保存方向游标；缺失方向禁用；无总页/跳页/加载更多/无限滚动。 | `A11` 四类游标响应与四类禁止入口/请求。 |
| `DT-PAG-04.a`、`DT-PAG-04.b`、`DT-PAG-04.c` | 页大小变化回起点并建快照；控件具名/显值；拒绝非法值。 | `A12` 25/30 两路径的状态、DOM 与请求。 |
| `DT-PAG-05.a`、`DT-PAG-05.b`、`DT-PAG-05.c`、`DT-PAG-05.d`、`DT-PAG-05.e` | 页码回最近有效页；游标只用服务端 fallback/初始；恢复一次；公告一次；不循环。 | `A12` 两模式及重复失效响应的四项计数和焦点。 |
| `DT-DATA-01.a`、`DT-DATA-01.b`、`DT-DATA-01.c` | 仅首次无结果用骨架；结果 busy；骨架无假操作数据。 | `A13` 初次场景的 DOM/ARIA 与控件数。 |
| `DT-DATA-02.a`、`DT-DATA-02.b`、`DT-DATA-02.c`、`DT-DATA-02.d`、`DT-DATA-02.e` | 刷新保留四类上下文；phase/busy 正确；同意图合并；旧行不冒充新响应。 | `A13` 刷新前后状态、DOM 和重复触发计数。 |
| `DT-DATA-03.a`、`DT-DATA-03.b`、`DT-DATA-03.c` | 首次失败进入专属阶段；结果区域错误/重试替代表格；重试新代次。 | `A14` 首次失败 DOM、owner 和重试日志。 |
| `DT-DATA-04.a`、`DT-DATA-04.b`、`DT-DATA-04.c`、`DT-DATA-04.d` | 刷新失败保留旧行并 stale；显示过期消息/重试；不清上下文。 | `A14` `R0`、分页、焦点、DOM 与 retry。 |
| `DT-DATA-05.a`、`DT-DATA-05.b`、`DT-DATA-05.c` | 筛选零结果有专属说明/恢复；空数据集匹配档位；不共用含糊文案。 | `A15` 两个 total=0 场景的状态、文案和入口。 |
| `DT-DATA-06.a`、`DT-DATA-06.b`、`DT-DATA-06.c`、`DT-DATA-06.d`、`DT-DATA-06.e` | 查询开始、数量/位置变化、失效页恢复、查询失败各由结果 owner 公告一次；完整错误不重复。 | `A13` 开始/结果变化；`A12` 恢复；`A14` 失败；`A15` owner 去重，逐事件断言计数 1。 |
| `DT-COL-01.a`、`DT-COL-01.b`、`DT-COL-01.c` | 三类列状态只用稳定 ID；隐藏同步移除视觉/关联/键盘；恢复语义且无旧引用。 | `A16` 隐藏/显示后的 DOM、ARIA 和 Grid 目标。 |
| `DT-COL-02.a`、`DT-COL-02.b`、`DT-COL-02.c` | 关键列标 required；不能隐藏；控制器禁用且说明原因。 | `A16` 关键列拒绝、DOM 与事件。 |
| `DT-COL-03.a`、`DT-COL-03.b`、`DT-COL-03.c`、`DT-COL-03.d` | 排序、焦点、打开操作依赖先迁移；无安全目标拒绝。 | `A16` 四类列；`A21` 先 focus 后移除顺序。 |
| `DT-COL-04.a`、`DT-COL-04.b`、`DT-COL-04.c` | 固定列不遮挡六类目标；检测后解最低优先级固定；列仍可见。 | `A17` 滚动/菜单/错误/焦点与自动解固定日志。 |
| `DT-COL-05.a`、`DT-COL-05.b`、`DT-COL-05.c`、`DT-COL-05.d` | 声明宽度边界/步长；指针和键盘截停；Home/End 到边界；暴露可访问值。 | `A17` 两输入方式与调整柄 DOM/ARIA。 |
| `DT-COL-06.a`、`DT-COL-06.b`、`DT-COL-06.c` | 无拖拽列排序；无个人持久化；展示变化不查询。 | `A01` 两排除入口；`A16`、`A17` 请求数 0。 |
| `DT-SEM-01.a`、`DT-SEM-01.b`、`DT-SEM-01.c`、`DT-SEM-01.d` | 两类普通表格用原生 table；有名称/说明；header 关联；不为样式改 Grid。 | `A18` 两实例可访问性树与根元素。 |
| `DT-SEM-02.a`、`DT-SEM-02.b`、`DT-SEM-02.c` | 原生 Table 不接管六类键；Tab 只进真实控件且顺序一致；静态 cell 不可 Tab。 | `A18` 键盘 handler 与 focus 计数。 |
| `DT-SEM-03.a`、`DT-SEM-03.b`、`DT-SEM-03.c` | 仅二维导航/选择用 Grid；采用即实现完整模型；不只贴 role。 | `A19` 合法 Grid 与无需求回退 Table。 |
| `DT-SEM-04.a`、`DT-SEM-04.b`、`DT-SEM-04.c`、`DT-SEM-04.d`、`DT-SEM-04.e`、`DT-SEM-04.f` | 单一 Tab/active；四方向、行 Home/End、全表 Home/End、Page 键确定；每键唯一目标。 | `A19` 全键序列、边界与 active 计数。 |
| `DT-SEM-05.a`、`DT-SEM-05.b`、`DT-SEM-05.c`、`DT-SEM-05.d`、`DT-SEM-05.e` | 只以按钮/菜单触发器验证；Enter/F2 入控件；Escape 回 cell；控件保留自身键盘；Tab 离开且不遍历静态 cell。 | `A19` 按钮/菜单场景、零编辑控件/handler/状态与焦点日志。 |
| `DT-FOC-01.a`、`DT-FOC-01.b` | 稳定目标存在时保持/恢复一次；不用数组索引替代记录。 | `A20` R7 换索引后的 activeElement 与 focus 计数。 |
| `DT-FOC-02.a`、`DT-FOC-02.b`、`DT-FOC-02.c` | 精确目标消失按四级 fallback；目标存活/可感知/相关；不落根/旧节点。 | `A20` 三轮刷新；`A21` 各事件最终目标。 |
| `DT-FOC-03.a`、`DT-FOC-03.b` | 草稿/筛选/排序请求不抢存活焦点；目标移除才迁移一次。 | `A21` 保留与移除两组场景。 |
| `DT-FOC-04.a`、`DT-FOC-04.b`、`DT-FOC-04.c` | 翻页记录来源；匹配响应按策略 focus 一次；自动恢复不二次抢焦。 | `A10`、`A11`、`A12`、`A21` 的 focus 日志。 |
| `DT-REPORT-01.a`、`DT-REPORT-01.b`、`DT-REPORT-01.c` | 未运行即标未验证；列所需环境；静态检查不冒充运行时。 | `A22` 每项环境/结果/证据记录与 not-run 计数。 |

## 可执行验收

每个检查记录 `{ownerId, snapshotId, requestGeneration, event, sourceId, targetId, recordId, columnId, phase, accepted, reason}`。异步检查要能手动控制响应顺序；DOM 检查使用真实可聚焦元素与可访问性树。以下断言是静态文档之外的可执行契约。

### `A01` 能力档位与范围

- **初始状态**：分别创建显式声明为 `display`、`row-action`、`bulk-action` 的三个 live owner；同时提供互相冲突的页面名称、无关复选框和后端能力提示，并经普通配置、隐藏配置、实验 flag 和未声明键盘事件四条路径注入所有第一版排除能力。
- **事件序列**：渲染三个实例并尝试各自允许的详情、单行操作和选择入口，改变页面名称、无关复选框和后端提示后再次渲染；随后经四条注入路径逐项尝试进入行内编辑、展开树表层级、拖拽行排序、切换透视表、点击“加载更多”、触发无限滚动哨兵、拖拽改变列顺序和保存个人布局。
- **预期状态**：三个 `resolvedTier` 与声明一致；`display` 不产生选择/操作状态，`row-action` 只产生单行意图，`bulk-action` 仅出现待后续扩展的选择/操作槽。行内编辑、树表、行拖拽、透视、加载更多、无限滚动、列顺序拖拽和布局持久化八项能力的状态写入分别为 0。
- **DOM / ARIA**：`display` 没有选择列、选择控件和批量工具栏；`row-action` 没有多选/全选/批量工具栏且整行不是单一大链接；只有 `bulk-action` 有对应结构。结果表格/Grid 子树内的行内编辑 `input`、`textarea`、`contenteditable` 与编辑按钮数均为 0；`treegrid`、行拖拽柄、透视控制、“加载更多”、无限滚动哨兵、列顺序拖拽柄和布局持久化入口分别不存在。
- **事件日志**：每实例只按显式声明记录一条 `tier-resolved`，页面名称、无关复选框和后端提示变化均不改变它；八项排除能力的普通配置、隐藏配置、实验 flag 和未声明键盘路径分别断言 handler 调用数为 0、请求数为 0、状态写入数为 0，其中行内编辑 handler 和编辑状态写入单独计数并均为 0。

### `A02` 权限降级

- **初始状态**：`bulk-action` 表格有选择列、可用批量工具栏，焦点位于将失权的控件。
- **事件序列**：提交权限变化，把 `resolvedTier` 依次降为 `row-action`、`display`。
- **预期状态**：每次降级原子移除越权交互状态；焦点按 `DT-FOC-02` 仅迁移一次，保留查询和可读结果。
- **DOM / ARIA**：没有空工具栏、空选择列、仅解释不提供恢复路径的 disabled 控件或指向已移除节点的 ARIA 引用。
- **事件日志**：每次降级各一条 `tier-downgraded` 和最多一条 `focus-migrated`；旧控件后续事件写入为 0。

### `A03` 四组状态与阶段

- **初始状态**：为四组状态提供可区分的状态写入入口与转换审计；实现可以使用任意框架或状态机制，检查不依赖 reducer、store 或特定 API。
- **事件序列**：依次发起查询、调整列宽、移动焦点、触发单行意图、让查询失败。
- **预期状态**：每个事件只改所属状态及明确派生值；请求阶段依次出现 `initial-loading` / `ready` 或 `initial-error`，已有结果刷新时出现 `refreshing` / `refresh-error`。
- **DOM / ARIA**：查询错误只在结果 owner；操作区域、筛选字段和列控制不会错误显示同一查询错误。
- **事件日志**：任何跨 owner 未声明写入令检查失败；不得只记录笼统 `loading=true` 或 `error=true`。

### `A04` 快速请求竞态

- **初始状态**：live owner 为 `O1`，当前结果 `R0`、generation 0。
- **事件序列**：连续提交筛选 `S1`、排序 `S2`、翻页 `S3`，得到 generation 1/2/3 和三个不可变快照；按 `S2 → S1 → S3` 返回，再交付一个 ownerId 为 `O0` 的迟到响应；另重复一次已取消的 `S1` 响应。
- **预期状态**：只有 generation 3、snapshot `S3`、owner `O1` 的响应更新行、分页、错误和阶段；所有快照内容在发出后保持字节/语义不变。
- **DOM / ARIA**：最终只显示 `S3` 行与摘要，`aria-busy` 随 `S3` 终态解除；旧错误或结果不闪回。
- **事件日志**：三次请求各一条 `request-started`；`S3` 一条 `response-accepted`，其余逐条 `response-discarded` 并记录 generation / snapshot / owner 不匹配原因；取消不改变结论。

### `A05` 查询事件矩阵

- **初始状态**：记录当前快照、generation、页码 4（另跑一次游标中段）、筛选草稿和已应用条件。
- **事件序列**：编辑草稿、改变焦点、开关详情、调整列；再依次应用筛选、提交排序、翻页、改变页大小；双击显式刷新并在第一次仍 in-flight 时再用键盘触发同一刷新；触发失效页恢复；待刷新终结后再次显式刷新。
- **预期状态**：前四项 generation 与请求数不变；应用筛选、排序、翻页、页大小、第一次刷新、失效页恢复和终结后的刷新各建立一个不可变快照并使 generation 恰好加一。in-flight 的第二次同意图刷新在创建快照前合并，`snapshotId`、generation、请求和开始公告均保持第一次刷新后的值。筛选/排序/页大小按规则回到起点，普通翻页使用目标位置。
- **DOM / ARIA**：草稿编辑不改变结果摘要；提交事件后摘要标识新条件正在加载，不能把旧行标成已匹配新条件。
- **事件日志**：非查询事件 `request-started=0`；每个被接受的查询意图 `snapshot-created=1`、`request-started=1`；第二次同意图刷新只记录 `query-intent-merged=1`，并断言 `snapshot-created=0`、generation 增量 0、`request-started=0`、开始公告 0。

### `A06` 筛选草稿、应用、重置与 URL

- **初始状态**：`defaultFilters={status: active}`，一个 `immediate` 的已提交状态值、一个 `explicit` 的多字段草稿，并含 `urlSafe`、`sensitive` 与自由文本敏感值。
- **事件序列**：只编辑复合控件内部 query / active option；提交 immediate 值；编辑 explicit 草稿后点击应用；点击重置两次；序列化 URL。
- **预期状态**：内部草稿不改表格 `filterDraft` / `appliedFilters` 或请求；immediate 仅在字段提交值后应用；explicit 仅点击应用后生效；第一次重置恢复 `defaultFilters` 并请求，第二次语义相同不请求。
- **DOM / ARIA**：结果摘要只显示已应用条件；URL 只含 `urlSafe` 值，路径、查询串、片段、标题和分析事件中均无敏感值。
- **事件日志**：合法值每次应用恰好一个快照/请求；未提交 query、无变化重置和非法字段值请求数为 0。

### `A07` 已应用条件可见与移除

- **初始状态**：有三个已应用条件，其中一个筛选控件折叠。
- **事件序列**：聚焦条件摘要，分别移除可见条件和折叠条件。
- **预期状态**：每次移除更新 `appliedFilters`，回到分页起点并请求；其他条件保留。
- **DOM / ARIA**：三个条件均有可访问名称和独立移除按钮；折叠条件仍在摘要中可发现；移除后不残留陈旧描述关系。
- **事件日志**：每次 `filter-removed`、`snapshot-created`、`request-started` 各 1。

### `A08` 稳定排序与跨页完整性

- **初始状态**：数据含相同业务排序值、null、不同大小写和区域字符；场景一提供不可变服务端快照 `datasetVersion=V1` 并保存 V1 的完整记录 ID 集合，场景二不提供一致性版本能力。
- **事件序列**：在 V1 内按声明的空值/大小写/区域规则和稳定次序键取完所有页并重复查询；随后让下一页返回 `V2`。在无版本场景中，于两页之间插入或删除一条会改变页边界的记录并交付变化信号；最后提交另一排序。
- **预期状态**：V1 内相同主值始终按稳定次序键排列、重复查询顺序相同，且只在 V1 内断言跨页零重复遗漏。收到 V2 或检测到无版本场景的页间数据变化时，当前分页链失效、`stale: true`、停止拼接/继续导航并回到第一个有效位置重新开始；不得对该链声称零重复遗漏。新排序仍回到第一个有效位置。
- **DOM / ARIA**：V1 结果摘要和排序状态与实际顺序一致；版本失配/页间变化后显示数据已变化或可能过期及重新开始状态，不把不同版本页面呈现为同一完整集合。
- **事件日志**：每个快照记录完整排序和 `datasetVersion`/一致性能力；V1 跨页 ID 并集等于 V1 基线集合、交集无重复、遗漏数和重复数均为 0。V2/无版本变化各记录一次 `pagination-chain-invalidated`、一次 `stale-set` 和一次从首个有效位置重启，失效后 `page-appended=0`。

### `A09` 单列/多列排序语义

- **初始状态**：一列不可排序、两列可排序；分别以 `single`、`multiple` 渲染。
- **事件序列**：键盘激活各表头排序按钮，循环方向并在多列模式改变优先级。
- **预期状态**：single 新列替换旧业务排序；multiple 保留明确优先级；不可排序列不触发事件。
- **DOM / ARIA**：按钮有“按 X 升序/降序”等名称；不可排序列无按钮和 `aria-sort`；原生表格只有主排序列设置正确 `aria-sort`，多列其余优先级由可访问文本说明。
- **事件日志**：每次有效提交一个 `sort-committed` 和一个请求；不可排序列请求数为 0。

### `A10` 页码分页边界

- **初始状态**：可靠总数 95、pageSize 10；分别位于第 1、5、10 页。
- **事件序列**：键盘激活上一页、下一页、直接页码和越界页码；控制响应返回。
- **预期状态**：合法目标建立新快照；首页上一页、末页下一页和越界目标不请求；匹配响应后按 `DT-FOC-04` 聚焦。
- **DOM / ARIA**：显示当前页、10 总页和结果范围；边界按钮为原生 disabled；当前页以 `aria-current="page"` 或等价语义标识。
- **事件日志**：每个合法目标一个 `page-change` / 请求 / 结果焦点事件；边界和越界请求为 0。

### `A11` 游标分页边界

- **初始状态**：分别构造仅 next、prev+next、仅 prev、无游标四种响应。
- **事件序列**：激活上一页/下一页并返回对应游标结果；尝试随机跳页、加载更多和滚到底自动请求。
- **预期状态**：只使用服务端提供的方向游标；缺失方向不请求；不存在总页数或随机位置状态。
- **DOM / ARIA**：只有上一页/下一页，缺失方向原生 disabled；没有虚构页码、加载更多或无限滚动哨兵。
- **事件日志**：每次允许导航一个请求并记录使用的游标；随机跳页、加载更多、滚动触发请求均为 0。

### `A12` 页大小与失效页恢复

- **初始状态**：页码位于第 6 页，另建游标中段；配置允许页大小 10/25/50。
- **事件序列**：选择 25；提交非法 30；删除导致页码末页变 4；让游标服务端返回当前位置失效及最近有效位置；重复送达失效响应。
- **预期状态**：25 回到起点并请求，30 保持旧值且不请求；页码只恢复到第 4 页一次，游标只回退一次；重复响应不再循环。
- **DOM / ARIA**：页大小控件有名称和当前值；恢复后可见新位置并仅一次播报；焦点按来源策略只移动一次。
- **事件日志**：每次有效页大小变化一个请求；每次失效场景 `invalid-page-detected=1`、`page-recovery-requested=1`、`page-recovered=1`、由结果 owner 发出的 `page-recovered-announcement=1`，重复失效响应不得增加任一计数。

### `A13` 首次加载与刷新

- **初始状态**：场景一无结果；场景二有上次成功行、分页和焦点意图。
- **事件序列**：开始首次加载并提交改变结果数量/位置的成功响应；开始刷新，在第一次仍 in-flight 时用点击、Enter 和事件重放重复触发同一刷新；提交改变结果数量/位置的新结果；终结后再刷新一次。
- **预期状态**：首次为 `initial-loading`；刷新为 `refreshing` 且旧上下文保留；同一 in-flight 查询意图的三次重复触发均在创建快照前合并，第二次及后续触发的 `snapshotId`、generation、请求和开始公告均不变；匹配成功后为 `ready`、`stale: false`，终结后的新刷新才创建下一快照/代次/请求。
- **DOM / ARIA**：首次仅有不可操作骨架且结果容器 `aria-busy=true`；刷新仍显示旧行并 busy；提交后替换为新行且 busy=false。
- **事件日志**：每个被接受的查询意图 `snapshot-created=1`、`request-started=1`、由结果 owner 发出的 `query-start-announcement=1`；每个已接受且改变数量/位置的成功响应 `result-change-announcement=1`。三次重复触发各为 `query-intent-merged=1`，并分别断言 snapshot 增量 0、generation 增量 0、请求增量 0、开始公告增量 0；骨架中的链接、按钮和选择控件数为 0。

### `A14` 初次失败与刷新失败

- **初始状态**：分别从无结果首次加载和有 `R0` 的刷新开始。
- **事件序列**：返回失败；聚焦并激活重试；让重试成功。
- **预期状态**：首次失败为 `initial-error` 且无假表格；刷新失败为 `refresh-error`、`stale: true` 并保留 `R0`、分页和焦点意图；重试使用新代次。
- **DOM / ARIA**：错误和重试位于结果 owner；刷新错误明确说明可能过期；同一完整错误只出现/公告一次。
- **事件日志**：每个被接受的失败响应 `query-failed=1`，且由结果 owner 发出的 `query-failed-announcement=1`；同一失败在其他公告 owner 的完整消息计数为 0。每次重试一个新快照/代次/请求，旧响应不能清错。

### `A15` 筛选零结果、空数据集与错误 owner

- **初始状态**：同一实例分别返回 `(total=0, appliedFilters 非默认)` 与 `(total=0, 无有效筛选)`；另注入字段错误、查询错误和操作错误。
- **事件序列**：提交结果并激活各恢复入口。
- **预期状态**：前者进入筛选零结果并可调整/清除条件，后者进入独立空数据集状态；三类错误只写各自 owner。
- **DOM / ARIA**：零结果文案提及当前条件，空数据集文案匹配能力档位；完整错误不在字段、结果和操作区重复。
- **事件日志**：每个改变结果数量/位置的已接受响应由结果 owner 发出 `result-change-announcement=1`；错误审计中每个 `errorId` 只有一个 `primaryOwnerId` 和一条完整公告，筛选字段、结果摘要和全局 live region 其余两处的同一完整消息计数均为 0。

### `A16` 列显示与依赖迁移

- **初始状态**：稳定列 ID 包含关键列、普通列、当前排序列、焦点列和已打开操作列。
- **事件序列**：尝试隐藏关键列；依次隐藏普通列、排序列、焦点列和操作列；再显示；调整密度。
- **预期状态**：关键列被拒绝并说明原因；普通列直接隐藏；其余列只有在排序取消/迁移、焦点迁移、操作关闭/迁移成功后隐藏。所有展示变化不改快照/generation。
- **DOM / ARIA**：隐藏列的 header/cell/ARIA 引用和 Grid 导航目标一致消失；恢复后 header 关联有效，无陈旧 DOM ID；关键列控制为可感知 disabled。
- **事件日志**：每次依赖按 `dependency-migrated → column-hidden` 排序；无法迁移时只有 `column-hide-rejected`；查询请求数为 0。

### `A17` 固定列与键盘宽度

- **初始状态**：窄容器含多列固定列、长内容与一个 `min=120,max=360,step=20` 的可调列。
- **事件序列**：横向滚动并聚焦边界单元格、打开菜单/错误；制造遮挡；对调整柄使用左右键、Home、End，并用指针越界拖动。
- **预期状态**：遮挡时解除最低优先级固定状态但列仍可见；宽度始终截停在 120–360，Home=120、End=360；不创建查询。
- **DOM / ARIA**：焦点环、菜单、错误和滚动边界不被固定列覆盖；调整柄可聚焦且暴露列名、当前值、最小/最大值。
- **事件日志**：每次实际宽度变化一个 `column-width-changed`；自动解固定一个 `column-unpinned-for-visibility`；请求数为 0。

### `A18` 原生 Table 语义与键盘

- **初始状态**：分别渲染只读表格和含链接、按钮、行菜单触发器的 `row-action` 表格。
- **事件序列**：用可访问性树检查名称/header 关联；从表格前一路 Tab 到表格后；按方向键、Home/End/Page Up/Page Down。
- **预期状态**：焦点只进入真实控件并保持视觉/DOM 顺序；表格不拦截导航键；行菜单和行跳转各自触发，不互相触发。
- **DOM / ARIA**：根为 `<table>`，有名称/说明，`th` scope 或等价关联正确；静态 cell 无 `tabindex=0`；整行不是包含多个控件的大链接。
- **事件日志**：静态单元格 focus 数为 0；方向/分页键的表格自定义 handler 数为 0；每个控件激活只触发自己的动作一次。

### `A19` ARIA Grid 语义与键盘

- **初始状态**：以明确二维导航需求渲染 20×8 Grid，其中部分单元格只含按钮或菜单触发器；另渲染一个无二维需求却请求 Grid 的配置。
- **事件序列**：Tab 进入；逐项执行四方向、Home/End、Ctrl/Command+Home/End、Page Up/Down；Enter/F2 把焦点移入按钮或菜单触发器，使用 Enter/Space 和菜单方向键执行控件自身行为，再以 Escape 返回单元格；Tab/Shift+Tab 离开。
- **预期状态**：所有 Grid 导航键得到规则定义的唯一目标并在边界截停；按钮/菜单触发器获得焦点后保持自身原生键盘语义；无二维需求的配置回退原生 Table。整个场景不建立行内编辑会话或编辑状态。
- **DOM / ARIA**：Grid 有名称及正确 row/column/gridcell 语义；任一时刻只有一个活动单元格/单一 Tab 入口，active 引用只指向已渲染节点；Grid 结果子树内的行内编辑 `input`、`textarea`、`contenteditable` 和编辑按钮数为 0。
- **事件日志**：每键最多一个 `grid-active-cell-changed`；焦点进入按钮/菜单和 Escape 返回各一次；静态单元格不进入逐个 Tab 顺序；行内编辑 handler 调用数和编辑状态写入数均为 0。

### `A20` 稳定标识焦点恢复

- **初始状态**：焦点位于 `{recordId:R7,columnId:status,controlId:details}`，记录其数组索引。
- **事件序列**：刷新后把 R7 移到另一索引但保留目标；再刷新并移除精确控件；再移除 R7。
- **预期状态**：第一次保持/恢复 R7 的同一逻辑控件；第二次移到 R7 的等价控件；第三次按策略移到 status 列最近记录或结果摘要，均只一次。
- **DOM / ARIA**：每次目标均存活、可聚焦并具有名称；焦点从不落到旧索引的其他记录、body、文档根或已移除节点。
- **事件日志**：每轮最多一条 `focus-restored` / `focus-migrated`，记录稳定 ID 和选择该 fallback 的原因。

### `A21` 事件来源与目标消失

- **初始状态**：分别把焦点放在筛选应用按钮、排序按钮、分页按钮、将隐藏列控件和将因权限降级移除的控件。
- **事件序列**：提交对应事件并控制结果到达；令一半场景保留原节点、一半移除目标；为分页触发失效页自动恢复。
- **预期状态**：筛选/排序发请求不抢仍存活焦点；目标消失才迁移一次。翻页匹配响应后按声明策略聚焦摘要/标题或保留语义未变的触发器；自动恢复不再二次聚焦。列隐藏与权限降级先迁移再移除。
- **DOM / ARIA**：最终 activeElement 是事件相关、存活且有名称的目标；无指向已移除列/控件的 ARIA 引用。
- **事件日志**：每个用户意图最多一次最终 `focus`；旧响应、失效页恢复和依赖清理均不得增加计数。

### `A22` 验收覆盖与运行时报告

- **初始状态**：收集本文件全部 `DT-*` 规则、其“硬规则分句覆盖清单”和 `A01` 至 `A21` 的执行清单。
- **事件序列**：逐条人工拆分每项规则中的独立义务/禁止项，为每个分句分配唯一 clause ID，并核对它映射到具体可观察断言；再用结构检查确认每个规则至少有一个 clause、每个 clause 引用存在的验收。运行已具备环境的检查，并把未运行项标记状态。
- **预期状态**：不存在无 clause、无验收映射或只用规则 ID 冒充分句覆盖的硬规则；复合规则的每个独立分句均在覆盖清单中单列。浏览器、屏幕阅读器、键盘、真实数据竞态或组件运行时未实际执行的项目保持 `未验证`。
- **DOM / ARIA**：只有真实运行并保存断言结果的场景可标记通过；静态 `rg` 或 Markdown 审查不能替代交互/辅助技术结果。
- **事件日志**：报告为每项保存命令/环境、时间、结果和证据位置；不得把 `not-run` 计为 pass。

## 参考资料

- [WAI-ARIA Authoring Practices Guide: Grid Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/grid/)
- [WAI-ARIA Authoring Practices Guide: Table Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/table/)
- [WCAG 2.2](https://www.w3.org/TR/WCAG22/)
