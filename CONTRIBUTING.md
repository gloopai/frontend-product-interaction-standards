# 贡献指南

感谢你帮助完善前端产品交互规范。贡献应让规则更清晰、可验证，并保持其对不同技术栈的适用性。

参与社区前，请阅读并遵守[行为准则](CODE_OF_CONDUCT.md)。如发现安全问题，请遵循[安全政策](SECURITY.md)，不要在公开讨论中披露。

## 开始贡献

1. Fork 本仓库。
2. 让你的 Fork 与上游 `main` 保持同步。
3. 从 `main` 创建一个描述清晰、短生命周期的分支，例如 `dialog-overlay-coverage`。
4. 在该分支完成一个聚焦的改动；不要将无关格式化、重命名或其他主题混入同一改动。

## 编写或修改规则

规则描述的是用户可观察到的产品行为与验收条件，而不是某个框架、组件库或项目的实现细节。请使用框架无关的产品语言；可以说明必须达到的效果、边界和验证方式，但不要把 Vue、React、Tailwind 或某个组件的具体写法当成通用产品规则。

新增或改变 Skill 行为时，按以下顺序工作：

1. 先在**未加载本 Skill**的相同场景建立失败基线，明确当前会出现的错误行为或无法满足的验收条件。
2. 只加入解决该问题所需的最小规则。
3. 使用完全相同的场景复测，确认规则能引导出预期行为，同时不引入不相关限制。

通用路由和核心原则保留在 `SKILL.md`。某一交互类别的具体规则放入职责单一的 `references/<category>.md`，并在 `SKILL.md` 的“规范路由”中增加或更新入口。不要在多个文件复制同一条完整规则；参考文件应是该类别的唯一事实来源。

## 本地验证

提交前，请在仓库根目录运行以下检查，并在 PR 中说明结果。官方 `quick_validate.py` 依赖 `PyYAML`；如果当前 Python 环境尚未提供该依赖，请创建隔离环境并在其中安装：

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install PyYAML
python ~/.codex/skills/.system/skill-creator/scripts/quick_validate.py .
```

如果当前 Python 环境已经安装 `PyYAML`，可直接在仓库根目录运行：

```bash
python3 ~/.codex/skills/.system/skill-creator/scripts/quick_validate.py .
```

同时进行 Markdown 相对链接检查，确认文档中不以 `http://`、`https://`、`mailto:` 或 `#` 开头的链接均解析到现有文件。扫描新增或修改的文档，确保不含未完成的占位标记。最后运行：

```bash
git diff --check
```

若规则涉及可交互界面，还应按规则要求实际验证相关行为和视口；不要把尚未执行的验证写成已通过。

## 提交与 Pull Request

每个提交和 Pull Request 应只覆盖一个可审查的主题。提交前检查差异，避免包含构建产物、个人配置或无关改动。

提交 Pull Request 时，请说明：

- 改动的动机与所解决的问题；
- 规则或路由带来的行为变化；
- 失败基线、复测及上述本地验证的证据；
- 对既有使用者、现有规则或兼容性的影响；若没有影响，也请明确说明。

维护者可能要求补充场景、收窄规则或调整文档结构，以保持规范可执行且不与既有规则冲突。
